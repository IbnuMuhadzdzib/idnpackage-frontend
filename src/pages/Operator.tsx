import { useEffect } from "react";

function Operator() {

    useEffect(() => {
            document.title = "Operator - IDN Paketku";
        }, []);

    return(
        <main>

        </main>
    )
}

export default Operator